name: swrly
description: Server-state cache for Flutter — dedupe, cache by query key, stale-while-revalidate. A TanStack Query for Flutter.
version: 0.2.1
homepage: https://github.com/redhotsixbull/swrly
repository: https://github.com/redhotsixbull/swrly
issue_tracker: https://github.com/redhotsixbull/swrly/issues

topics:
  - state-management
  - cache
  - http
  - async
  - networking

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
