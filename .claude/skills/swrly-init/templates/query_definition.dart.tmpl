// {{FILE_PATH}} — generated by swrly-init.
// See doc/CONVENTIONS.md for the rules this file follows.

import 'package:swrly/swrly.dart';
// import 'package:{{HTTP_PACKAGE}}/{{HTTP_PACKAGE}}.dart';

/// Replace with a real query from your app. Each Query holds a stable
/// (key, fn) pair — put every query used across your app in this folder
/// (doc/CONVENTIONS.md §5).
final exampleQuery = Query<Map<String, dynamic>>(
  key: const ['example'],
  fn: () async {
    // Wire this to your existing HTTP client.
    // final response = await httpClient.get(...);
    // return response.data;
    throw UnimplementedError('Replace this with a real fetcher.');
  },
  // Never leave staleTime unset — see doc/CONVENTIONS.md §6.
  staleTime: const Duration(seconds: 30),
);

/// For parameterised queries, use QueryFamily:
///
/// final userQuery = QueryFamily<User, int>(
///   prefix: const ['user'],
///   fn: (id) => api.getUser(id),
///   staleTime: const Duration(minutes: 1),
/// );
